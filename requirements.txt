# Flask REST API (src/ layout)
Flask>=3.0.0,<4
flask-cors>=4.0.0
SQLAlchemy>=2.0.0,<3
alembic>=1.13.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
# Pinned <24: gunicorn 24+ removed the `eventlet` worker class entirely (no more
# gunicorn.workers.geventlet module), which Socket.IO's real-time chat relies on.
gunicorn>=21.0.0,<24
redis>=5.0.0
PyJWT>=2.8.0
cryptography>=42.0.0
bcrypt>=4.1.0
boto3>=1.34.0
firebase-admin>=6.5.0,<7
flask-socketio>=5.3.0,<6
eventlet>=0.35.0,<1
